# Code-only PUBLIC image (R + shiny + the app, NO data) → Umbrel pulls it from
# ghcr without any PAT. The unpublished data is mounted from the Umbrel app
# volume at /data (see deploy/ship_data.sh) and never enters the registry.
services:
  app_proxy:
    environment:
      APP_HOST: planning-gr-sc-atac-explorer_web_1
      APP_PORT: 3838
      # No Umbrel login on this app: access is gated by Tailscale (tailnet
      # membership + node share + port-scoped ACL), not an Umbrel account.
      PROXY_AUTH_ADD: "false"

  web:
    image: ghcr.io/asdggr/sc-atac-explorer:1.0.1
    restart: on-failure
    environment:
      DATA_DIR: /data
    volumes:
      - ${APP_DATA_DIR}/data:/data
    stop_grace_period: 1m
